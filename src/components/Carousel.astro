---

---

<head>
  <meta charset="UTF-8" />
  <meta name="description" content="Free Web tutorials" />
  <meta name="keywords" content="HTML, CSS, JavaScript" />
  <meta name="author" content="John Doe" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
</head>
<body>
  <!-- aria-label, det her er et label for screen readers   -->
  <section aria-label="Newest Photos">
    <div class="carousel" data-carousel>
      <button class="carousel_button prev" data-carousel-button="prev"
        ><svg id="Layer_1" xmlns="http://www.w3.org/2000/svg" width="50.66" height="104" viewBox="0 0 87.66 104"
          ><defs></defs><path
            class="cls-1"
            d="M69.9,55.53l-2.81-7.33L49.33,0h-11.28l-18.32,48.2-2.81,7.33L0,104h8.46c1.54-3.27,4.5-8.51,9.94-13.35,4.24-3.77,8.47-5.96,11.48-7.22,3.58-.68,7.88-1.24,12.77-1.32,5.29-.09,9.93.39,13.73,1.04,2.84,1.02,7.88,3.27,12.67,8.02,4.87,4.84,7.15,9.98,8.18,12.83h10.43l-17.76-48.47Z"
          ></path></svg
        ></button
      >
      <button class="carousel_button next" data-carousel-button="next"
        ><svg id="Layer_2" xmlns="http://www.w3.org/2000/svg" width="50.66" height="104" viewBox="0 0 87.66 104"
          ><defs></defs><path
            class="cls-1"
            d="M69.9,55.53l-2.81-7.33L49.33,0h-11.28l-18.32,48.2-2.81,7.33L0,104h8.46c1.54-3.27,4.5-8.51,9.94-13.35,4.24-3.77,8.47-5.96,11.48-7.22,3.58-.68,7.88-1.24,12.77-1.32,5.29-.09,9.93.39,13.73,1.04,2.84,1.02,7.88,3.27,12.67,8.02,4.87,4.84,7.15,9.98,8.18,12.83h10.43l-17.76-48.47Z"
          ></path></svg
        ></button
      >
      <ul data-slides>
        <li class="slide" data-active>
          <!-- brug supabase til at gøre de nemmere at skifte billeder -->
          <img class="heroBig" src="public/img/frontpage_hero1.webp" alt="Billede af en kunster der synger" />
          <img class="heroSmall" src="public/img/heroNWS1.webp" alt="Billede af en kunster der synger" />
        </li>
        <li class="slide">
          <!-- brug supabase til at gøre de nemmere at skifte billeder -->
          <img class="heroBig" src="public/img/heroNW2.webp" alt="Billede af en kunster der synger" />
          <img class="heroSmall" src="public/img/heroNWS2.webp" alt="Billede af en kunster der synger" />
        </li>
        <li class="slide">
          <!-- brug supabase til at gøre de nemmere at skifte billeder -->
          <img class="heroBig" src="public/img/heroNW3.webp" alt="Billede af en kunster der synger" />
          <img class="heroSmall" src="public/img/heroNWS3.webp" alt="Billede af en kunster der synger" />
        </li>
        <li class="slide">
          <!-- brug supabase til at gøre de nemmere at skifte billeder -->
          <img class="heroBig" src="public/img/heroNW4.webp" alt="Billede af en kunster der synger" />
          <img class="heroSmall" src="public/img/heroNWS4.webp" alt="Billede af en kunster der synger" />
        </li>
      </ul>
      <div class="carousel_nav">
        <ul class="dotsData">
          <li><button role="button" aria-label="1 hero picture" class="carousel_indicator current-slide" data-dot="0"></button></li>
          <li><button role="button" aria-label="1 hero picture" class="carousel_indicator" data-dot="1"></button></li>
          <li><button role="button" aria-label="1 hero picture" class="carousel_indicator" data-dot="2"></button></li>
          <li><button role="button" aria-label="1 hero picture" class="carousel_indicator" data-dot="3"></button></li>
        </ul>
      </div>
    </div>
  </section>
</body>

<style>
  /* Dette skal slettes når vi sætter det ind i index */
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }
  body {
    margin: 0;
  }
  /* Alt i mellem denne tekst og over */

  .slide .heroSmall {
    display: none;
  }
  .carousel {
    width: 100vw;
    height: 80vh;
    position: relative;
  }

  .carousel > ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: 200ms opacity ease-in-out;
    transition-delay: 200ms;
  }

  .slide > img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
  .slide[data-active] {
    opacity: 1;
    transition-delay: 0;
  }

  .carousel_button {
    position: absolute;
    z-index: 2;
    background: none;
    border: none;

    top: 50%;
    transform: translateY(-50%);

    cursor: pointer;
  }
  #Layer_2 {
    rotate: 90deg;
  }

  #Layer_1 {
    rotate: -90deg;
  }
  .cls-1 {
    fill: #292929;
    stroke-width: 0px;
  }
  .cls-1:hover,
  .cls-1:focus {
    fill: var(--accentColor);
  }

  .carousel_button.prev {
    left: 1rem;
  }

  .carousel_button.next {
    right: 1rem;
  }

  .carousel_nav {
    background-color: var(--accentColor);
    display: flex;
    justify-content: center;
    padding: 10px 0;
    z-index: 100;
  }

  .carousel_indicator {
    border: 0;
    border-radius: 50%;
    width: 20px;
    height: 20px;
    background: rgba(0, 0, 0, 0.3);
    margin: 0 12px;
    cursor: pointer;
    transition: 0.3s ease;
  }

  .carousel_indicator.current-slide {
    background: rgba(189, 110, 41, 0.75);
  }

  .dotsData {
    display: flex;
    flex-direction: row;
    z-index: 3;
    position: relative;
    top: 70vh;
  }
  .dotsData li {
    list-style: none;
  }
  /* Her skal billederne være størrer så der ikke er hvist omkring dem */

  @media screen and (max-width: 700px) {
    .carousel {
      height: 40vh;
    }

    .dotsData {
      top: 30vh;
    }

    .carousel_indicator {
      width: 12px;
      height: 12px;
    }

    #Layer_1 {
      width: 2rem;
    }
    #Layer_2 {
      width: 2rem;
    }
    .carousel_button.prev {
      display: none;
    }

    .carousel_button.next {
      display: none;
    }

    .slide .heroSmall {
      display: block;
    }
    .slide .heroBig {
      display: none;
    }

    @media screen and (max-width: 482px) {
      .carousel {
        height: 30vh;
      }
      .dotsData {
        top: 2vh;
      }
    }
  }
</style>

<!-- defer = at script'et loader efter alt andet er loadet  -->
<script defer>
  const buttons = document.querySelectorAll("[data-carousel-button]");
  const dots = document.querySelectorAll(".carousel_indicator");

  buttons.forEach((button) => {
    button.addEventListener("click", () => {
      const offset = button.dataset.carouselButton === "next" ? 1 : -1;
      const slides = button.closest("[data-carousel]").querySelector("[data-slides]");

      const activeSlide = slides.querySelector("[data-active]");
      let newIndex = [...slides.children].indexOf(activeSlide) + offset;

      if (newIndex < 0) newIndex = slides.children.length - 1;
      if (newIndex >= slides.children.length) newIndex = 0;

      slides.children[newIndex].dataset.active = true;
      delete activeSlide.dataset.active;

      updateDots(newIndex);
    });
  });

  dots.forEach((dot, index) => {
    dot.addEventListener("click", () => {
      const slides = document.querySelector("[data-slides]");
      const activeSlide = slides.querySelector("[data-active]");

      slides.children[index].dataset.active = true;
      delete activeSlide.dataset.active;

      updateDots(index);
    });
  });

  function updateDots(activeIndex) {
    dots.forEach((dot, index) => {
      if (index === activeIndex) {
        dot.classList.add("current-slide");
      } else {
        dot.classList.remove("current-slide");
      }
    });
  }

  let autoSlideInterval = setInterval(() => {
    const slides = document.querySelector("[data-slides]");
    const activeSlide = slides.querySelector("[data-active]");
    let newIndex = [...slides.children].indexOf(activeSlide) + 1;

    if (newIndex >= slides.children.length) newIndex = 0;

    slides.children[newIndex].dataset.active = true;
    delete activeSlide.dataset.active;

    updateDots(newIndex);
  }, 7000); // 7000 milliseconds = 7 seconds

  function resetAutoSlide() {
    clearInterval(autoSlideInterval);
    autoSlideInterval = setInterval(() => {
      const slides = document.querySelector("[data-slides]");
      const activeSlide = slides.querySelector("[data-active]");
      let newIndex = [...slides.children].indexOf(activeSlide) + 1;

      if (newIndex >= slides.children.length) newIndex = 0;

      slides.children[newIndex].dataset.active = true;
      delete activeSlide.dataset.active;

      updateDots(newIndex);
    }, 7000);
  }
</script>
